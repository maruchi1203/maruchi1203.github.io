name: Deploy Frontend To GitHub Pages

on:
  push:
    branches: ["main"]
    paths:
      - "src/frontend/**"
      - ".github/workflows/deploy-frontend-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_API_BASE_URL: ${{ vars.NEXT_PUBLIC_API_BASE_URL }}
      NEXT_PUBLIC_GITHUB_USER_NAME: ${{ vars.NEXT_PUBLIC_GITHUB_USER_NAME }}
      NEXT_PUBLIC_VELOG_NAME: ${{ vars.NEXT_PUBLIC_VELOG_NAME }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: src/frontend/package-lock.json

      - name: Install dependencies
        run: npm ci --prefix src/frontend --workspaces=false

      - name: Install missing Node types for Next.js type check
        run: npm install --prefix src/frontend --no-save @types/node@^22

      - name: Build static site
        run: npm run build --prefix src/frontend

      - name: Add .nojekyll
        run: touch src/frontend/out/.nojekyll

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: src/frontend/out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
